<odoo>
    <data>
        <record model="ir.ui.view" id="view_newteacher_kanban">
            <field name="name">ims.newteacher.kanban</field>
            <field name="model">hr.employee</field>           
            <field name="inherit_id" ref="hr.hr_kanban_view_employees"/> 
            <field name="arch" type="xml">  
                <xpath expr="//field[@name='category_ids']" position="attributes">
                    <attribute name="name">roles</attribute>
                </xpath>

                <!-- <field name="category_ids" widget="many2many_tags" options="{'color_field': 'color'}" groups="hr.group_hr_user"/> -->

            </field>		      
        </record>
    </data>
</odoo>