<odoo>
    <data>
        <record model="ir.ui.view" id="view_ims_company_kanban">
            <field name="name">ims.company.kanban</field>
            <field name="model">ims.company</field>
            <field name="mode">primary</field>      
            <field name="inherit_id" ref="ims.view_ims_person_kanban"/> 
            <field name="arch" type="xml">  
                <xpath expr="//img[@id='photo']" position="attributes">
                    <!-- TODO: this is not working and I tried everything. Even writing the entire view (without inheriting) -->
                    <attribute name="t-att-src">kanban_image('ims.company', 'image', record.id.raw_value)</attribute>
                </xpath>                 
            </field>		      
        </record>
    </data>
</odoo>