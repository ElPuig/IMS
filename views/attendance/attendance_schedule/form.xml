<odoo>
    <data>    
        <!-- TODO: accessible from session form, everything should be readonly (or disable the link)  -->
        <!-- <record model="ir.ui.view" id="view_attendance_group_form">
            <field name="name">ims.attendance.group.form</field>
            <field name="model">ims.attendance_group</field>
            <field name="arch" type="xml">
                <form string="Template group">
                    <sheet>                        
                        <group string="Basic data">
                            <field name="end_date"/>
                            <field name="duration"/>
                            <field name="teacher"/>
                            <field name="student"/>
                            <field name="study"/>
                            <field name="subject"/>
                            <field name="space"/>
                            <field name="weekday"/>
                            <field name="start_time" widget="float_time" />
                            <field name="end_time" widget="float_time" />
                            <field name="color" widget="color_picker" />
                            <field name="group"/>
                            <field name="has_templates" invisible="1"/>                                                                              
                        </group>
                        <notebook>
                            <page string="Templates">
                                <field name="attendance_templates" string="" context="{'show_attribute': False}">
                                    TODO: this sould be automatically filled when selecting the group.
                                    <list editable="bottom">
                                        <field name="student" />
                                    </list>
                                </field>
                            </page>
                            <page string="Sessions">
                                <field name="attendance_sessions" widget="one2many_list" context="{'show_attribute': False}">
                                    <list default_order="date desc">                                  
                                        <field name="weekday"/>
                                        <field name="date" widget="date"/>
                                        <field name="start_time"/>
                                        <field name="end_time"/>
                                        <field name="notes"/>
                                    </list>
                                </field>
                            </page>                                                        
                        </notebook>
                        <button name="generate_templates_by_student" 
                            string="Generate Templates" 
                            type="object"
                            class="oe_highlight"
                            attrs="{'invisible': [('has_templates', '=', True)]}"
                            context="{'active_id': active_id, 'active_model': active_model}"
                        />
                        <button name="generate_next_session" 
                            string="Generate Next Session" 
                            type="object"
                            class="oe_highlight"                            
                            context="{'active_id': active_id, 'active_model': active_model}"
                        />                    
                    </sheet>	
                </form>
            </field>			
        </record> -->
    </data>
</odoo>